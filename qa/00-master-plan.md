# MLO-Calc — Production-Scale Local Data Testing

Goal: Build sanitized production-scale local data under production-like settings; inventory every
user-facing feature/role/route/button/input/modal/state/workflow; define acceptance criteria +
finite risk-based edge cases; test as a real user with reproduction evidence; fix with regression
tests; rerun to a clean pass.

Session scope (confirmed with user): **Full fix+test authority** (overrides read-only CLAUDE.md
for this session). Test method: **run the app live** (Flutter web, Chrome).

## Environment
- Flutter (stable), Dart 3.10.7 on Windows x64. Package name: `loan_ranger`.
- Devices: Chrome/Edge web, Android device. Live run target: **web-server @ 127.0.0.1:8080**.
- Persistence: `SharedPreferences` (prefs) + `flutter_secure_storage` (secure). On web these map to
  localStorage / IndexedDB-backed shims — a known risk area to verify live.

## Baseline health (captured before any change)
- `flutter pub get`: OK (53 transitive packages have newer incompatible versions — informational).
- `flutter analyze`: **6 issues, 0 errors** (unused import in a test, directive ordering, underscore
  local, depend_on_referenced_packages in `bin/` helper scripts). Non-blocking.
- `flutter test`: **254/254 pass**. Unit + widget + golden coverage is solid at the logic level.

Implication: defects are most likely in (a) live-web platform behavior, (b) cross-feature
integration/workflows, (c) untested edge cases, (d) data persistence at scale.

## Route / surface map
Primary bottom-nav / rail tabs: Calculator, Amortization, Qualification, Analysis, History.
Workspace-only features: Loan Programs, Rent vs Buy.
Pushed routes: Workspace Dashboard, Settings, Calculator Layout Preview, ARM Wizard,
Loan Program Editor, Comparison, PDF Report Preview.
Dialogs/sheets: Closing Costs sheet, Info ("How to Use") dialog, NLP/Voice dialog, Share Quote dialog.
App-bar actions: Share quote (calculator tab only), Voice/Text (NLP), More menu → Settings /
How to Use / Workspace Dashboard / Loan Programs / Rent vs Buy.
Adaptive layout breakpoints (main.dart): rail ≥900, extended rail ≥1200, compact app bar <700.

## Inventory files (per-area, generated by subagents)
- `qa/inventory/01-calculator-amortization.md`
- `qa/inventory/02-qualification-analysis-rentbuy-programs.md`
- `qa/inventory/03-arm-comparison-reporting-history-share.md`
- `qa/inventory/04-settings-workspace-navigation-bootstrap.md`

## Bug log
- `qa/bugs/` — one file per confirmed bug with reproduction + evidence.
- `qa/bugs/BUGLOG.md` — index with severity + status.

## Test phases
1. [done] Baseline health.
2. [in progress] Inventory + acceptance criteria + edge cases.
3. Seed production-scale sanitized local data.
4. Live user testing per workflow; log bugs w/ evidence.
5. Triage shared causes/dependencies; fix with regression tests.
6. Rerun full inventory to clean pass or blocked handoff.
