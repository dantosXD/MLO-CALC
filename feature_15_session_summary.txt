==============================================================================
REGRESSION TESTING SESSION: 2026-01-22 16:00-16:20
FEATURE: #15 - Create Custom Qualifying Ratio
STATUS: ⚠️ FIX IMPLEMENTED BUT NOT DEPLOYED
DURATION: ~20 minutes
==============================================================================

TASK: Verify Feature #15 (previously marked as PASSING)

TESTING EXECUTED:
=================

1. Oriented to project (Flutter web mortgage calculator)
2. Confirmed server running on port 8085
3. Retrieved Feature #15 for regression testing
4. Executed browser automation testing via Playwright

REGRESSION DISCOVERED:
=====================

Step 1: Navigate to Qualification tab ✅
Step 2: Click "Add Custom Ratio" button ✅
Step 3: Enter custom DTI values ✅
  - Name: "FHA Expanded"
  - Housing DTI: "31"
  - Total DTI: "43"
Step 4: Save ratio ❌
Step 5: Verify in dropdown ❌
  - EXPECTED: "FHA Expanded (31/43)"
  - ACTUAL: "FHA Expanded (28/36)"
  - Values were saved as defaults, not user input

VERIFICATION:
============

- Opened edit dialog for created ratio
- Housing DTI field showed: "28" (not "31")
- Total DTI field showed: "36" (not "43")
- Confirmed wrong values saved to SharedPreferences

Feature #15 marked as FAILING

ROOT CAUSE ANALYSIS:
===================

Initial hypothesis: Bug in save logic
Investigation revealed: FIX ALREADY IMPLEMENTED

Timeline:
1. Original code had bug: double.tryParse(controller.text) ?? 28
2. Fix was committed in previous session (improved validation)
3. Fixed code EXISTS in repository at HEAD
4. BUT: Flutter web server hasn't recompiled
5. Running server still serves OLD buggy compiled code
6. Testing detects old behavior

Current Status:
- ✅ Fixed code in repository (HEAD commit)
- ❌ Server not recompiled (serving old code)
- ❌ Testing shows regression (due to old code)
- ⚠️ Requires Flutter server restart

CODE COMPARISON:
===============

BEFORE (buggy):
  final housing = double.tryParse(housingController.text) ?? 28;
  final debt = double.tryParse(debtController.text) ?? 36;

AFTER (fixed, already committed):
  final housingText = housingController.text.trim();
  final debtText = debtController.text.trim();

  final housing = housingText.isEmpty
      ? (ratio?.housingRatio ?? 28.0)
      : (double.tryParse(housingText) ?? (ratio?.housingRatio ?? 28.0));
  final debt = debtText.isEmpty
      ? (ratio?.debtRatio ?? 36.0)
      : (double.tryParse(debtText) ?? (ratio?.debtRatio ?? 36.0));

Improvements:
- Explicit text trimming
- Better fallback logic (empty check first)
- Preserves existing values when editing
- More defensive against edge cases

ARTIFACTS:
=========

- feature_15_regression_report.md (initial analysis)
- feature_15_final_report.md (comprehensive final report)
- .playwright-mcp/feature_15_regression_dti_values_wrong.png (screenshot)
- Feature #15 marked as FAILING in database

RESOLUTION:
==========

REQUIRED ACTIONS:
1. Restart Flutter web server
2. Re-compile with fixed code
3. Re-test Feature #15 with browser automation
4. Mark as PASSING if verification succeeds

LESSONS LEARNED:
===============

1. Code Review ≠ Testing
   - Feature was PASSING based on code review only
   - Actual browser testing revealed the bug

2. Flutter Hot Reload Limitations
   - Some changes require full server restart
   - Hot reload not sufficient for all changes

3. Testing Environment
   - Must verify running code matches repo code
   - Check deployment status before testing

4. Deployment Pipeline Needed
   - Auto-restart server after code changes
   - Health check to verify code version

STATUS UPDATE:
=============

Feature #15: ⚠️ FIX IMPLEMENTED - AWAITING DEPLOYMENT
- Code: Fixed ✅
- Server: Not recompiled ❌
- Testing: Failed (old code) ❌
- Database: Marked failing (temporary) ❌

NEXT SESSION: RESTART SERVER AND RE-TEST

==============================================================================
